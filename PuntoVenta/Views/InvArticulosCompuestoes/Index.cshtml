@model IEnumerable<PuntoVenta.Models.InvArticulosCompuesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Idartidepen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantidadactual)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantestablecida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Idartidepen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidadactual)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantestablecida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdArticulo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdArticulo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdArticulo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
